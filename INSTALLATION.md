# 📦 تعليمات التثبيت والتشغيل

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- **Node.js** (الإصدار 18.0 أو أحدث)
- **npm** أو **yarn** أو **pnpm**
- محرر أكواد مثل **VS Code**

## خطوات التثبيت

### 1. تثبيت المكتبات

```bash
npm install
```

أو باستخدام yarn:

```bash
yarn install
```

أو باستخدام pnpm:

```bash
pnpm install
```

### 2. إعداد متغيرات البيئة (اختياري)

انسخ ملف `.env.example` إلى `.env.local`:

```bash
cp .env.example .env.local
```

ثم قم بتعديل القيم حسب احتياجاتك.

### 3. تشغيل الخادم المحلي

```bash
npm run dev
```

سيعمل التطبيق على: `http://localhost:3000`

### 4. بناء المشروع للإنتاج

```bash
npm run build
npm run start
```

## هيكل المشروع

```
├── app/                    # صفحات التطبيق (App Router)
│   ├── page.tsx           # الصفحة الرئيسية (Dashboard)
│   ├── branches/          # إدارة الفروع
│   ├── menu/              # إدارة المنيو
│   ├── campaigns/         # الحملات والعروض
│   ├── analytics/         # التحليلات المتقدمة
│   ├── ai-assistant/      # المساعد الذكي
│   ├── assets/            # مكتبة الأصول
│   ├── staff/             # إدارة الموظفين
│   ├── reports/           # التقارير
│   ├── layout.tsx         # Layout رئيسي
│   └── globals.css        # ستايلات عامة
├── components/            # المكونات المشتركة
│   ├── Sidebar.tsx        # القائمة الجانبية
│   ├── Header.tsx         # رأس الصفحة
│   ├── DashboardLayout.tsx # Layout لوحة التحكم
│   └── StatCard.tsx       # بطاقة الإحصائيات
├── public/                # الملفات الثابتة
├── package.json           # إعدادات المشروع
├── tsconfig.json          # إعدادات TypeScript
├── tailwind.config.js     # إعدادات Tailwind
└── next.config.js         # إعدادات Next.js
```

## الصفحات المتاحة

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| لوحة التحكم | `/` | الصفحة الرئيسية مع الإحصائيات |
| الفروع | `/branches` | إدارة الفروع والمواقع |
| المنيو | `/menu` | إدارة قائمة الطعام |
| الحملات | `/campaigns` | إنشاء وإدارة العروض |
| التحليلات | `/analytics` | تحليلات متقدمة |
| المساعد الذكي | `/ai-assistant` | AI Chef Assistant |
| مكتبة الأصول | `/assets` | إدارة الصور والملفات |
| الموظفين | `/staff` | إدارة الفريق والصلاحيات |
| التقارير | `/reports` | التقارير الدورية |

## التخصيص

### تغيير الألوان

عدّل ملف `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: {
        500: '#3b82f6', // اللون الأساسي
        // ... بقية التدرجات
      },
    },
  },
}
```

### إضافة صفحة جديدة

1. أنشئ مجلداً جديداً في `app/`
2. أضف ملف `page.tsx` داخل المجلد
3. استخدم `DashboardLayout` للتصميم المتناسق

مثال:

```typescript
import DashboardLayout from '@/components/DashboardLayout'

export default function NewPage() {
  return (
    <DashboardLayout>
      <h1>صفحة جديدة</h1>
    </DashboardLayout>
  )
}
```

## التكامل مع API

لإضافة تكامل مع Backend:

1. أنشئ مجلد `app/api/` للـ API Routes
2. استخدم `fetch` أو `axios` لاستدعاء البيانات
3. استخدم `useState` و `useEffect` لإدارة الحالة

## التوزيع (Deployment)

### Vercel (موصى به)

```bash
npm install -g vercel
vercel
```

### خوادم أخرى

```bash
npm run build
```

ثم انسخ محتويات `.next/` إلى الخادم.

## الدعم الفني

في حالة وجود أي مشاكل:

1. تأكد من تثبيت Node.js بشكل صحيح
2. احذف مجلد `node_modules` و `.next` ثم أعد التثبيت
3. تأكد من عدم وجود تعارض في المنافذ (Port)

## المساهمة

هذا المشروع مفتوح للتطوير. يمكنك:

- إضافة ميزات جديدة
- تحسين التصميم
- إصلاح الأخطاء
- تحسين الأداء

## الترخيص

جميع الحقوق محفوظة © 2025


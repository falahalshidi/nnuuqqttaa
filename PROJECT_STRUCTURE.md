# 🏗️ هيكل المشروع

## 📁 نظرة عامة

```
restaurant-dashboard/
│
├── 📂 app/                          # Next.js App Router
│   ├── 📄 layout.tsx               # Layout رئيسي للتطبيق
│   ├── 📄 page.tsx                 # الصفحة الرئيسية (Dashboard)
│   ├── 📄 globals.css              # الأنماط العامة
│   │
│   ├── 📂 branches/                # إدارة الفروع
│   │   └── 📄 page.tsx            # صفحة الفروع
│   │
│   ├── 📂 menu/                    # إدارة المنيو
│   │   └── 📄 page.tsx            # صفحة المنيو
│   │
│   ├── 📂 campaigns/               # الحملات والعروض
│   │   └── 📄 page.tsx            # صفحة الحملات
│   │
│   ├── 📂 analytics/               # التحليلات المتقدمة
│   │   └── 📄 page.tsx            # صفحة التحليلات
│   │
│   ├── 📂 ai-assistant/            # المساعد الذكي
│   │   └── 📄 page.tsx            # صفحة AI Chef
│   │
│   ├── 📂 assets/                  # مكتبة الأصول
│   │   └── 📄 page.tsx            # صفحة الأصول
│   │
│   ├── 📂 staff/                   # إدارة الموظفين
│   │   └── 📄 page.tsx            # صفحة الموظفين
│   │
│   └── 📂 reports/                 # التقارير
│       └── 📄 page.tsx            # صفحة التقارير
│
├── 📂 components/                   # المكونات المشتركة
│   ├── 📄 Sidebar.tsx              # القائمة الجانبية
│   ├── 📄 Header.tsx               # رأس الصفحة
│   ├── 📄 DashboardLayout.tsx      # Layout لوحة التحكم
│   ├── 📄 StatCard.tsx             # بطاقة الإحصائيات
│   └── 📄 Chart.tsx                # مكون المخططات
│
├── 📂 public/                       # الملفات الثابتة
│   ├── 🖼️ images/                  # الصور
│   ├── 🎨 icons/                   # الأيقونات
│   └── 📄 favicon.ico              # أيقونة الموقع
│
├── 📄 package.json                  # إعدادات المشروع والمكتبات
├── 📄 tsconfig.json                 # إعدادات TypeScript
├── 📄 tailwind.config.js            # إعدادات Tailwind CSS
├── 📄 postcss.config.js             # إعدادات PostCSS
├── 📄 next.config.js                # إعدادات Next.js
├── 📄 .gitignore                    # ملفات مستثناة من Git
│
├── 📄 README.md                     # نظرة عامة عن المشروع
├── 📄 INSTALLATION.md               # تعليمات التثبيت المفصلة
├── 📄 FEATURES.md                   # دليل الميزات الكامل
├── 📄 QUICKSTART.md                 # دليل البدء السريع
└── 📄 PROJECT_STRUCTURE.md          # هذا الملف
```

---

## 🔍 شرح مفصل

### 📂 `app/`

دليل App Router الخاص بـ Next.js 14. كل مجلد فرعي يمثل مساراً (route) في التطبيق.

#### 📄 `layout.tsx`
```typescript
// Layout رئيسي يحتوي على:
- <html> و <body>
- Metadata للصفحة
- خطوط Google Fonts
- دعم RTL للعربية
```

#### 📄 `page.tsx`
```typescript
// الصفحة الرئيسية تحتوي على:
- عدادات الأداء (Stat Cards)
- مخططات بيانية (Charts)
- قائمة الأطباق الأكثر مشاهدة
- التنبيهات الذكية
```

#### 📄 `globals.css`
```css
/* يحتوي على: */
- استيراد خط Cairo
- Tailwind directives
- Classes مخصصة (card, btn-primary, etc.)
- تنسيق Scrollbar
- Animations
```

### 📂 `app/branches/`

```typescript
// صفحة إدارة الفروع تحتوي على:
- عرض شبكي للفروع
- خريطة تفاعلية
- حالة كل فرع (نشط/مغلق/مشغول)
- إحصائيات كل فرع
- إجراءات سريعة (تعديل/تعطيل)
```

**المكونات الرئيسية:**
- Interface: `Branch` - تعريف نوع الفرع
- State: `selectedBranch` - الفرع المحدد
- State: `viewMode` - طريقة العرض (شبكة/خريطة)

### 📂 `app/menu/`

```typescript
// صفحة إدارة المنيو تحتوي على:
- عرض شبكي/قائمة للأطباق
- فلترة حسب الفئة
- بحث فوري
- إحصائيات الأطباق
- نسخ الأسعار الجماعي
```

**المكونات الرئيسية:**
- Interface: `MenuItem` - تعريف نوع الطبق
- State: `viewMode` - طريقة العرض
- State: `selectedCategory` - الفئة المحددة
- State: `searchQuery` - نص البحث

### 📂 `app/campaigns/`

```typescript
// صفحة الحملات تحتوي على:
- قائمة الحملات النشطة والمجدولة
- صانع عروض ذكي
- تقويم الحملات
- تتبع الأداء في الوقت الحقيقي
```

**المكونات الرئيسية:**
- Interface: `Campaign` - تعريف نوع الحملة
- State: `selectedCampaign` - الحملة المحددة
- Functions: معالجات حالة الحملة

### 📂 `app/analytics/`

```typescript
// صفحة التحليلات تحتوي على:
- مخططات تفاعلية (Recharts)
- توزيع جغرافي وزمني
- مقارنة الفروع
- مؤشرات أداء Radar
- رؤى ذكية
```

**المكونات الرئيسية:**
- Charts: LineChart, PieChart, BarChart, RadarChart
- State: `timeRange` - الفترة الزمنية
- State: `selectedMetric` - المؤشر المحدد

### 📂 `app/ai-assistant/`

```typescript
// صفحة المساعد الذكي تحتوي على:
- رؤى وتوصيات AI
- توليد محتوى تسويقي
- تحليل المراجعات
- محادثة AI تفاعلية
```

**المكونات الرئيسية:**
- Interface: `AIInsight` - تعريف نوع الرؤية
- State: `activeTab` - التبويب النشط
- State: `chatMessage` - رسالة الدردشة

### 📂 `app/assets/`

```typescript
// مكتبة الأصول تحتوي على:
- رفع وإدارة الملفات
- عرض شبكي/قائمة
- تنبيهات جودة
- تتبع الاستخدام
```

**المكونات الرئيسية:**
- Interface: `Asset` - تعريف نوع الملف
- State: `viewMode` - طريقة العرض
- State: `selectedAssets` - الملفات المحددة

### 📂 `app/staff/`

```typescript
// إدارة الموظفين تحتوي على:
- قائمة الموظفين
- إدارة الأدوار والصلاحيات
- سجل النشاط
- سجل التدقيق
```

**المكونات الرئيسية:**
- Interface: `Staff` - تعريف نوع الموظف
- Interface: `Role` - تعريف نوع الدور
- State: `selectedRole` - الدور المحدد

### 📂 `app/reports/`

```typescript
// صفحة التقارير تحتوي على:
- قوالب تقارير جاهزة
- تقارير سابقة
- جدولة تلقائية
- تصدير متعدد (PDF/Email)
```

---

## 🧩 المكونات المشتركة

### 📄 `Sidebar.tsx`

```typescript
// القائمة الجانبية تحتوي على:
- Logo المطعم
- قائمة التنقل
- مؤشر الصفحة النشطة
- ملف المستخدم
```

**Props:** لا يوجد  
**State:** `pathname` من `usePathname()`

### 📄 `Header.tsx`

```typescript
// رأس الصفحة يحتوي على:
- شريط البحث
- أيقونة الإشعارات
- أزرار إجراءات سريعة
```

**Props:** لا يوجد  
**State:** `notifications` - عدد الإشعارات

### 📄 `DashboardLayout.tsx`

```typescript
// Layout لوحة التحكم:
- يجمع Sidebar + Header
- يضيف padding مناسب
- responsive للأجهزة المختلفة
```

**Props:**
- `children: React.ReactNode` - المحتوى

### 📄 `StatCard.tsx`

```typescript
// بطاقة الإحصائيات:
- عرض قيمة رقمية
- نسبة التغيير
- أيقونة
- ألوان حسب الاتجاه
```

**Props:**
- `title: string` - العنوان
- `value: string | number` - القيمة
- `change?: number` - نسبة التغيير
- `icon: LucideIcon` - الأيقونة
- `trend?: 'up' | 'down' | 'neutral'` - الاتجاه

---

## 🎨 التصميم

### نظام الألوان (Tailwind Config)

```javascript
primary: {
  50: '#eff6ff',   // أفتح درجة
  100: '#dbeafe',
  200: '#bfdbfe',
  300: '#93c5fd',
  400: '#60a5fa',
  500: '#3b82f6',  // اللون الأساسي
  600: '#2563eb',
  700: '#1d4ed8',
  800: '#1e40af',
  900: '#1e3a8a',  // أغمق درجة
}
```

### Classes المخصصة

```css
.card              // بطاقة بيضاء بظل خفيف
.btn-primary       // زر أساسي أزرق
.btn-secondary     // زر ثانوي outlined
.btn-ghost         // زر شفاف
.input-field       // حقل إدخال
.stat-card         // بطاقة إحصائيات
.dashboard-gauge   // عداد لوحة التحكم
```

---

## 📊 البيانات

### مصادر البيانات الحالية

حالياً، جميع البيانات **وهمية (Mock Data)** لأغراض العرض:

```typescript
// أمثلة:
const branches: Branch[] = [...]
const menuItems: MenuItem[] = [...]
const campaigns: Campaign[] = [...]
```

### للتكامل مع API حقيقي:

1. أنشئ مجلد `lib/api/`
2. أضف functions للاستدعاء:

```typescript
// lib/api/branches.ts
export async function getBranches() {
  const res = await fetch('/api/branches')
  return res.json()
}
```

3. استخدم في المكونات:

```typescript
const [branches, setBranches] = useState([])

useEffect(() => {
  getBranches().then(setBranches)
}, [])
```

---

## 🔧 ملفات الإعداد

### `package.json`
- اسم المشروع
- الإصدار
- Scripts (dev, build, start)
- Dependencies

### `tsconfig.json`
- إعدادات TypeScript
- Path aliases (@/*)
- Strict mode

### `tailwind.config.js`
- ألوان مخصصة
- خطوط
- Plugins

### `next.config.js`
- إعدادات Next.js
- i18n للعربية
- تحسينات الأداء

---

## 📦 المكتبات المستخدمة

| المكتبة | الغرض |
|---------|-------|
| **Next.js 14** | إطار React |
| **TypeScript** | النوعية الصارمة |
| **Tailwind CSS** | تصميم سريع |
| **Recharts** | المخططات البيانية |
| **Lucide React** | الأيقونات |
| **date-fns** | معالجة التواريخ |
| **clsx** | إدارة Classes |

---

## 🚀 الخطوات التالية

### للتطوير:

1. ✅ أضف API Routes في `app/api/`
2. ✅ اربط مع قاعدة بيانات
3. ✅ أضف Authentication
4. ✅ أضف تحميل الصور الحقيقي
5. ✅ أضف نظام الإشعارات
6. ✅ أضف Multi-language support

### للإنتاج:

1. ✅ راجع الأمان
2. ✅ اختبر على أجهزة مختلفة
3. ✅ تحسين الأداء
4. ✅ Deploy على Vercel/AWS

---

**💡 نصيحة:** استخدم هذا الملف كمرجع عند العمل على المشروع!

